from setuptools import setup
from setuptools.extension import Extension


setup(
    name='pyTracing',
    version='0.0.1',
    packages=['pytracing', 'pytracing.shapes', 'pytracing.geometry'],
    url='',
    license='',
    author='vincent',
    author_email='',
    description='',
)